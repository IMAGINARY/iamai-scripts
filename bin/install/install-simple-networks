#!/bin/bash

COMMITISH="feature/pasch-workshop"
REPO_URL="https://github.com/IMAGINARY/simple-networks.git"
INSTALL_DIR="/opt/simple-networks"

"${BASH_SOURCE%/*}/../internal/install-source-from-git" "$COMMITISH" "$REPO_URL" "$INSTALL_DIR"
exit $?
