#!/bin/bash

# cd into script directory
cd "$(dirname -- "$0")" || exit

# rebind the joysticks to ensure correct mapping
sudo --preserve-env=GRADIENT_DESCENT_JS0_SYS_PATH,GRADIENT_DESCENT_JS1_SYS_PATH ./gradient-descent/rebind-joysticks
